<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.itesm.difbueno">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DIFBUENO"
        tools:targetApi="31">

        <activity
            android:name=".View.inicia_sesion_curp"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".View.no_curp">
            <!-- Agregar más configuración si es necesario -->
        </activity>

        <activity
            android:name=".ViewModel.generar_qr">
            <!-- Agregar más configuración si es necesario -->
        </activity>

        <activity
            android:name=".View.menu_dif_app">

        </activity>
        <activity
            android:name=".Model.encuesta_de_satisfaccion">

        </activity>
        <activity
            android:name=".ViewModel.mapa_comedores_dif">

        </activity>

        <activity
            android:name=".Model.derechos_y_obligaciones_comensal">

        </activity>

    </application>

</manifest>
